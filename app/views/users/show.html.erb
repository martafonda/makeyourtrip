<div>
  <strong>Email:</strong>
  <%= @user.email %><br>
  <strong>Name:</strong>
  <%= @user.name %><br>
</div>
<%= link_to 'Edit User', edit_user_path(@user), class: 'button secondary' %> 
<%= link_to 'Destroy', user_path(@user), class: 'button secondary', method: :delete, data: { confirm: 'Are you sure?' } %>
</html>