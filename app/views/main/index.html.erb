<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MakeYourTrip APP - Plan your trip here!!</title>
</head>
<body>
  <article>
    
    <% if user_signed_in? %>
      <%= link_to 'Logout', destroy_user_facebook_session_path, method: 'delete' %>
      <%= link_to 'Signout', user_registration_path, :confirm => 'Are you sure you want to destroy your account?', :method => :delete, class: "button small" %>
    <% else %>
      <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
    <% end %>  
  </article>
</body>
</html>